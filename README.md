# 将插件放入 mdserver-web/plugins/cluster_manager/ 目录
# 在面板 → 软件管理 → 集群管理 → 安装

# 或通过命令行
cd /www/server/mdserver-web/plugins
git clone https://github.com/your-repo/cluster_manager.git
mw plugin install cluster_manager
